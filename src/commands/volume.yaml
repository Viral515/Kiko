# Команды управления громкостью системы
- id: volume_mute
  triggers:
    - "выключи звук"
    - "заглуши звук"
    - "тишина"
  action:
    type: script
    target: "scripts/utils/volume_control.ps1"
    args: ["mute"]
  responses:
    sound:
      - ok
    tts:

- id: volume_unmute
  triggers:
    - "включи звук"
    - "верни звук"
  action:
    type: script
    target: "scripts/utils/volume_control.ps1"
    args: ["unmute"]
  responses:
    sound:
      - ok
    tts: